spring:
  application:
    name: coreflow-backend

server:
  port: 8080

coreflow:
  api:
    base-path: /api
  fitness:
    ascend:
      base-url: ${ASCEND_API_BASE_URL}
      host: ${ASCEND_API_HOST}
      key: ${ASCEND_API_KEY}
